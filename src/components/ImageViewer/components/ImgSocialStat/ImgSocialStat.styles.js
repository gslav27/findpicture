import imageViewerStyles from '../../ImageViewer.styles';

const style = {
  socialData: {
    color: '#ccc',
    fontSize: '1.2em',
    marginLeft: '-6px',
  },
  toolBarBottom: {
    bottom: 0,
    justifyContent: 'center',
    paddingRight: '10px',
  },
  ...imageViewerStyles
}

export default style;